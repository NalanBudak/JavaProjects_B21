package day29_Methods;

public class _Methods_Nedir {

    /*

        METHOD ===> Function, grouping a series of code fragments.

          Advantage ==> Re-Usable and easy to maintain

        Main method ====> main method used for able to run code

        public   static    void    main ( String[] args)  {
		       	statements;
	        }
--------------------------------------------------------------------

	declaring method======> MUST be within class .(VERY IMPORTANT)


                        public       static      void          main    (String[]args){
	Syntax===>		AccessModifier  specifier   returnTYPE   MethodName (Parameter){
						statements
			}


			AccessModifier====>public, protected, default, private
								FOR NOW ONLY USE PUBLIC
							        public===> open to the world

			specifier=====> static, final, abstract, synchronized
							FOR NOW ONLY USE STATIC
						static========> can be called through the class name

						ClassName.MethodName
						Arrays.toString()
						Arrays.sort()

			returnType======> void, any dataType
			            	  void===>method does not return any data

			parameter====> any dataType

------------------------------------------------------------------------

without parameter: if all info are known for completing the functionality

		with parameter: additional information that's must needed for the method to complete the functionality

				if method passes parameter, we MUST give argument to the method when we call it


     */
}
